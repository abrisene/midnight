import { Canvas, Meta } from "@storybook/blocks";

import * as FileUploaderExample from "./file-uploader.stories";

<Meta of={FileUploaderExample} />

## FileUploader

The FileUploader component creates a file uploader.

<Canvas of={FileUploaderExample.Default} />
