import { Canvas, Meta } from "@storybook/blocks";

import * as MarqueeExample from "./marquee.stories";

<Meta of={MarqueeExample} />

## Marquee

The Marquee component creates a scrolling text or content effect.

<Canvas of={MarqueeExample.Default} />
