import { Canvas, Meta } from "@storybook/blocks";

import * as TreeViewExample from "./tree-view.stories";

<Meta of={TreeViewExample} />

## TreeView

The TreeView component renders a tree view of data.

<Canvas of={TreeViewExample.Default} />
