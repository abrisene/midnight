import { Canvas, Meta } from "@storybook/blocks";

import * as MultiSelectExample from "./multi-select.stories";

<Meta of={MultiSelectExample} />;

## MultiSelect

The MultiSelect component renders a multi-select input.

<Canvas of={MultiSelectExample.Default} />
