import { Canvas, Meta } from "@storybook/blocks";

import * as SkeletonBrowserExample from "./skeleton-browser.stories";

<Meta of={SkeletonBrowserExample} />

## SkeletonBrowser

The SkeletonBrowser component provides a skeleton structure for browser-like interfaces.

<Canvas of={SkeletonBrowserExample.Default} />
